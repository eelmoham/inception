version: '3'
services:
  node-app:
    container_name: express-node-app-container
    build:
      context: .
      args:
        - NODE_ENV=development
    volumes:
      - ./src:/app/src:ro
    environment:
      - NODE_ENV=development
    command: npm run start-dev
    